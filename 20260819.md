# 2026-07-21 TIL

## 오늘 한 것
- 콜로소 라는 사이트에서 레벨 디자인 관련된 강의 하나를 결제했다.
- 수업 시간에 배운 내용을 다시 한번 정리해보았다.

## 배운 것
#### Class Default Object(CDO)
- 언리얼 엔진에서 각 UClass 마다 존재하는 기본 객체(Default Instance)
```
UCLASS()
class AMyCharacter : public ACharacter
{
    GENERATED_BODY()

public:
    UPROPERTY(EditAnywhere)
    float Health = 100.0f;
};
```
- AMyCharacter 라는 클래스가 있으면 언리얼은 이 클래스에 대한 CDO를 만들어서 기본값을 가지고 있도록 관리.
- 클래스의 객체를 만들 때 기본적으로 어떤 값을 가져야 하는지를 저장하는 기준 객체 = CDO<br>

`AMyCharacter* Character = NewObject<AMyCharacter>();`
- 새로운 객체가 생성될 때 CDO에 들어있던 값을 바탕으로 기본 설정이 초기화된다.

  
| CDO | Instance(객체) |
|----------|-----|
| 하나만 존재 | 여러 개 가능 |
| 기본값 보관 | CDO 복제하여 생성 |
| 월드에 존재하지 않음 | 월드에 존재 |
| 수정하면 모든 새 인스턴스에 영향 | 독립적인 값|
| 엔진이 클래스 로딩/ 초기화 과정에서 생성 | NewObject, SpawnActor 등|

## 느낀 점
- 객체에 대한 공부가 더 필요하다고 느꼈다. CDO에 대한 개념도 잘 와닿지 않아 조금 더 추가 공부를 했더니 이해가 됐다.
