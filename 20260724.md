# 2026-07-24 TIL

## 오늘 한 것
- 저번 수업 복습
- 오늘도 ctrl+ f5 누르면 계속 오류가 떴다. 껐다 켰다 하면서 하면 되는데 두 번 연속 누를 때는 안되니 복잡하다.
- MoveTemp에 대해 공부하기!

## 배운 것
- `MoveTemp`는 객체를 복사하지 않고 소유권을 이동시키기 위한 언리얼 함수
  
  복사는 공간 차지도 많이 하고, 시간 및 비용이 많이 들 수 있음.
  
  `MoveTemp`는 메모리를 새로 만드는 것이 아닌 가지고 있던 것을 넘겨주는 방식 -> 훨씬 빠르다!
  
  대신 옮겨준 애는 값이 없는 상태(비어있는 상태)가 된다.
  
  예시)
  ```cpp
  FString Name = TEXT("Player");
  FString NewName = MoveTemp(Name);
  ```

  
- FTimerHelper 만드는 방법을 배웠다
  ```cpp
  #include "FTimerHelper.h"

  FTimerHandle FTimerHelper::Delay(const UWorld* world, float inRate, TFunction<void(void)>&& Callback)
  {
  	FTimerHandle timerHandle;
  
  	if (world == nullptr)
  	{
		timerHandle;
	}

	//
	world->GetTimerManager().SetTimer(
		timerHandle,
		MoveTemp(Callback),
		inRate,
		false);
	
	return timerHandle;
  }
  void FTimerHelper::ClearTimer(const UWorld* world, FTimerHandle timerHandle)
  {
	  world->GetTimerManager().ClearTimer(timerHandle);
  }
  ```
  
  ```cpp

  #pragma once

  #include "CoreMinimal.h"

  /**
   *
   */
  class BLUEPRINTRUNTIMESAVINGAND_API FTimerHelper
  {
  public:
	  static FTimerHandle Delay(const UWorld* world, float inRate, TFunction<void(void)>&& Callback);
	  static void ClearTimer(const UWorld* world, FTimerHandle timerHandle);
  };


## 느낀 점
- md 파일 작성이 생각보다 어렵다. 코드 작성할 때 칸을 맞춰쓰거나, 나눠서 두 개를 넣는 경우에도 자꾸 마음대로 작성되지 않아서 수정을 많이 했다
