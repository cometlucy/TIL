# 2026-07-29 TIL

## 오늘 한 것
- 팀프로젝트에 사용할 에셋을 찾아봤다.
- 헤더와 cpp 파일의 차이에 대해 공부하고, 헤더 앞부분 include 내용에 대해 공부했다.
- 깃허브에서 엔터를 치고 싶으면 괄호br괄호를 쓰거나 문장 뒤에 스페이스바 두 번을 누르면 된다!

## 배운 것
```cpp
MyGameModeBase.h
#pragma once

#include "CoreMinimal.h"
#include "GameFramework/GameModeBase.h"
#include "MyGameModeBase.generated.h"
```

- `#pragma once` : 이 헤더 파일이 한 번만 포함 되도록 하는 전처리기 지시문
  - 헤더 파일 안에서 MyGameModeBase.h 가 두 번 포함될 수 있는데, 그러면 컴파일 에러가 발생됨 (두 번 정의되기 때문)<br>
  - 그래서 `#pragma once`를 넣어 이 파일은 이미 읽었으니 두 번째부턴 읽을 필요 없어~ 라는 뜻을 부여해준다.

- `#include "CoreMinimal.h"` : 언리얼에서 가장 기본이 되는 헤더.    
  - 자주 사용하는 기본 자료형과 클래스들이 들어있다. (FString, FVector 등)  
  - 가장 앞에 와야 한다.

- `#include "GameFramework/GameModeBase.h"` : 상속 받을 클래스를 정의.  
  - 여기에선 `AGameModeBase` 클래스를 사용할 수 있도록 가져오는 것을 의미한다.

- `#include "MyGameModeBase.generated.h"` : 언리얼의 리플렉션 시스템이 자동으로 생성한 코드를 포함한다.
  - 무조건 제일 마지막에 와야한다. << 중요!!!
---
#### 헤더 파일과 cpp 파일의 차이
- 헤더 파일(.h) : 무엇을 가지고 있는지 (설계도, 선언)
- CPP 파일(.cpp) : 실제로 어떻게 동작하는지 함수 내용 작성 (구현)


## 느낀 점
- md에 조금씩 익숙해지곤 있다. 근데 어떻게 써야될지 고민하는 시간이 아직 좀 긴 거 같다. 너무 형식에 얽매이지 말고 그냥 편하게 쓰는 습관이 생기면 좋을 거 같다.
- 너무 당연해서 딱히 의문을 가지지 않았던 것들에 대해 공부해봤다. 학원이나 대학에서 들었던 거 같긴 한데 잘 기억이 나지 않아 직접 찾아보면서 정리해봤다.
