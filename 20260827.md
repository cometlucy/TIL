# 2026-08-27 TIL

## 오늘 한 것
- 저번 수업 때 이해가 덜 됐던 부분을 공부했다.

## 배운 것
```
staticMeshComponent = CreateDefaultSubobject<UStaticMeshComponent>(TEXT("staticMeshComponent"));
staticMeshComponent->SetupAttachment(GetRootComponent());

particleSystem = CreateDefaultSubobject<UParticleSystemComponent>(TEXT("particleSystem"));
particleSystem->SetupAttachment(GetRootComponent());
```
- 처음 작성했던 코드. 근데 이렇게 하니까 언리얼에서 스태틱 메시랑 파티클이 서로 다른 위치에 나오는 문제가 생겼었다.
```
staticMeshComponent = CreateDefaultSubobject<UStaticMeshComponent>(TEXT("staticMeshComponent"));
SetRootComponent(staticMeshComponent);

particleSystem = CreateDefaultSubobject<UParticleSystemComponent>(TEXT("particleSystem"));
particleSystem->SetupAttachment(GetRootComponent());
```
- SetRootComponent 를 이용해 파티클을 스태틱 메시 안으로 넣었다.

  <img width="297" height="83" alt="image" src="https://github.com/user-attachments/assets/772c9030-26d6-4bce-9692-3b6eb6b5aedd" />
- 원래는 왼쪽에 아래 삼각형 모양이 없었는데 생겼고, 이제 파티클이 스태틱 메시 위치에 나온다.

## 느낀 점
- 크게 차이가 없는 거 같은데도 결과 값이 엄청나게 다르니까 신기했다. 주의해야할 게 참 많은 것이 코딩인 거 같다. 좀 더 이해할 수 있으면 좋을 거 같다.
